"""Ontology mapping placeholders for MVP."""
from __future__ import annotations


def map_gene_to_hgnc(symbol: str) -> str:
    """TODO: Replace with HGNC resolver in post-MVP versions."""

    return symbol


def merge_synonym_label(label: str) -> str:
    """TODO: Replace with ontology-backed synonym normalization."""

    return label
